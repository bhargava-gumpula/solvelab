/** Average sizes shown across the app. Add sizes here, not in components. */
export const statsConfig = {
  averageSizes: [5, 12, 50, 100],
  /** Sizes shown in the compact timer summary. */
  timerSummarySizes: [5, 12, 100],
  /** Rolling series drawn on the progress chart. */
  chartRollingSizes: [5, 12, 100],
} as const;
