# Development log

Every implementation step is recorded here with its outcome. Commits provide the exact source changes. Entries describe actions, decisions, failures, and verification without retaining credentials or private machine details.

## Phase V0 — foundation (2026-09-12; session continues into 2026-09-13 UTC)

1. Read the full supplied product/engineering specification, including the version gates and V0 acceptance criteria.
2. Inspected the workspace. It had no application or Git repository. Selected V0 as the next logical phase; V1 timing is deliberately deferred until user review.
3. Read the Sites building and portable setup/preview guidance. User requests phase reviews and eventual deployment to an existing website, so this phase stays local and unpublished.
4. Chose App Router/React/strict TypeScript/Tailwind and the supplied Next-compatible starter. Its default runtime is Vinext; retain standard Next imports and document a native Next deployment path.
5. Selected a graphite/neutral design with restrained orange accents, tabular timer digits, desktop sidebar, mobile bottom navigation, and light/dark/system themes.
6. Searched 21st.dev for navigation/components. Selected its shadcn Sidebar composition; reuse the compatible bundled shadcn primitive and adapt the composition/tokens, with source attribution in DESIGN.md. No 21st MCP was available.
7. Initialized the starter and read its layout, primary route, styles, TypeScript settings, component APIs, and scripts.
8. Attempted locked dependency installation. Sandbox npm DNS access failed. Retried with network permission. The plugin path became unavailable; resumed using the identical project-local install:ci script.
9. Implemented centralized brand/navigation config, app providers, accessible shell, root-to-timer routing, and a clearly labeled timer design preview. No fabricated solve history or diagnosis was persisted.
10. Implemented responsive visual tokens and a timer-focused first surface. Timing instructions are design copy; V0 clearly states controls arrive in V1.
11. User requested a GitHub repo and a log of every step. Checked the existing GitHub CLI login and verified the proposed repository name is unused. Chose private visibility by default.
12. Added README, design provenance, and persistent AGENTS.md instructions requiring a log update for each step and a review pause between phases.
13. Initialized Git (sandbox write restriction required escalation) and created the private repository at github.com/bhargava-gumpula/solvelab. Connected origin.
14. Locked starter dependencies installed successfully. Added Dexie plus Vitest, fake-indexeddb, and Playwright for the required persistence tests and browser checks.
15. Started the supplied Vinext preview runner. It did not bind its configured port; a local readiness request confirmed the failure. Stopped it and tried native Next.js, which also matches the requested framework and future website portability.
16. Committed the initial shell and step log (81c9fd5) and pushed main to the private GitHub repository.
17. Native Next.js with Turbopack served an unexpected 404 despite discovering the timer route. Read the installed Next.js page convention docs; switching to its webpack compiler produced HTTP 200. The local preview was handed to Codex (open request queued).
18. Preserved the supplied product specification in docs/PRODUCT_SPECIFICATION.md. Next.js generated an additional AGENTS.md documentation block; retained it.
19. Made native Next.js/webpack the default development and build path; retained optional Sites scripts. Configured static export and trailing slashes for deployment portability. A public deployment has not been created.
20. Added strict domain types covering solves, sessions, settings, skills, exercises, milestones, algorithms, training, and diagnostic records. Added centralized, typed seed metadata without storing fake measurements.
21. Added Dexie schema v1 with eight stores and an idempotent transactional initializer. It creates only Main and default preferences. Added a validated solve repository as a data-layer contract for V1, deriving final time without mutating raw time.
22. Connected browser-only storage initialization, visible failure/retry handling, and a storage status panel. Theme preference uses next-themes device-local storage; other settings use IndexedDB.
23. Implemented all seven V0 routes: Timer, Coach, Train, Algorithms, Learn, Stats, Settings. Added honest phase notices, empty states, searchable/filterable set metadata, curriculum outlines, and light/dark/system controls.
24. Added responsive page layouts and error/not-found routes. Added unit tests for initialization, idempotency, save/edit/delete/reopen, invalid data, session isolation, and seed reference integrity. Browser checks follow.
25. Ran initial validation: TypeScript and ESLint passed; all 8 unit tests passed across 2 files. Synchronized the lockfile with the project name and required packages.
26. Inspected the actual preview in the Codex browser at 1440px. Confirmed the timer layout, then navigated to Settings and observed “Local database ready.” Tested light and system theme selection and inspected light-mode layout.
27. Increased undersized secondary labels to a 12px minimum before final visual checks. Inspected Settings at the requested 375px mobile breakpoint. A theme-persistence read immediately after reload returned before hydration; follow-up checks will wait for the database-ready UI before reading selected appearance.
28. Added architecture documentation, the migration policy, and deployment notes covering static export, future subpath configuration, origin-specific local data, and no offline-reload guarantee before PWA support.
29. The first production export build passed and generated all seven application routes plus root/not-found. Verified light-theme persistence after waiting for hydration; system selection and mobile navigation also worked.
30. Added the static preview server and Playwright suite for the requested breakpoints, route state, mobile catalog behavior, theme persistence/system preference, blocked storage, keyboard skip navigation, and unknown-route recovery.
31. The first Playwright run could not launch because its Chromium binary was not installed; no browser assertions executed. Requested the official Playwright Chromium download, then will rerun against a fresh export.
32. Browser checks confirmed catalog search/filter and no-results behavior. Fixed a React controlled/uncontrolled RadioGroup warning by keeping the theme selection controlled during hydration. Added aria-current to the mobile Settings link.
33. Observed a transient development-manifest read error while building with the dev server active; it recovered. Final build and browser testing will use the exported artifact to avoid development/build contention.

## Handoff — V0 wrap-up and V1 daily timer (2026-09-12 → 2026-09-13, new agent)

The previous session stopped mid-validation with most V0 work uncommitted and no validation report. A new agent picked up the checkout and the private GitHub repository. Following spec §96 ("V0 + Timer Foundation" is the first milestone), this phase finishes V0 and delivers V1.

34. Inspected the checkout, git history (one pushed commit, large uncommitted diff), docs and the running static preview. Baseline typecheck, lint and 8 unit tests passed. Committed the uncommitted V0 work unchanged as a snapshot (616134f) so nothing was lost before refactoring.
35. Removed starter scaffolding unrelated to SolveLab: ChatGPT sign-in helper, Cloudflare/Vinext/Wrangler adapter, Drizzle/D1 examples, Sites install scripts, unused shadcn components (calendar, carousel, input-otp, resizable, form, combobox) and their dependencies. Static export with native Next.js/webpack stays the only build path.
36. Added Prettier (with the Tailwind class sorter) and formatted the codebase; the V0 source was written as single long lines. Rewrote globals.css as design tokens plus Tailwind utilities, replacing ~200 bespoke classes. Kept the V0 palette (graphite, restrained orange). Added Geist Sans/Mono via next/font (self-hosted at build time).
37. Implemented `lib/cube`: a notation parser (outer, wide, slice, rotation moves; amounts; primes; grouping) and a 54-sticker cube engine using 3D sticker rotation so every move type shares one implementation. Verified against reference facelet strings and known group orders (R U = 105, sexy move = 6, T-perm = 2).
38. Implemented `lib/timer/engine.ts`, a pure state machine (idle → ready → running → stopped, with inspection) driven by input timestamps. Elapsed time is always stop − start from performance.now/event.timeStamp; requestAnimationFrame only repaints. WCA inspection: >15 s is +2, >17 s is DNF. The press that stops the timer must be released before new input counts.
39. Implemented `lib/stats`: separated mean, session mean, trimmed average and rolling average; AoN trims ceil(5%) per side (WCA for Ao5, csTimer convention above). Rolling averages use a sorted sliding window and are checked against brute force on 300 values with DNFs. Added median, sample σ, coefficient of variation, PB progression, activity/streak and chart series builders.
40. Storage: added schema v2 (session sortOrder, new timer preferences) with an upgrade function and a test that opens real v1 data. Split repositories into sessions (create, rename, archive, delete with cascade, never delete the last session), solves (add, edit penalty/notes/tags without touching raw time) and settings (defaults merged for old records).
41. Added versioned JSON backup/restore (`lib/export/backup.ts`) with full validation, referential checks, recomputed final times, merge vs replace, and a single transaction. Test forces a failure mid-restore and confirms nothing changed.
42. First scramble provider used cubing.js via a dynamic import. Unit tests passed in Node, but a browser check of the production build showed "Module worker instantiation failed" and a silent fallback to random-move scrambles: webpack copies cubing's worker entry without its dependencies. Resolved by pre-bundling the scrambler with esbuild (the bundler cubing.js supports) into `public/vendor/cubing`, loaded with a native import at runtime and configured to try the esbuild worker strategy first. Added an e2e test asserting random-state scrambles and no failed requests. The random-move fallback remains, clearly labeled in the UI.
43. Built the timer UI: session switcher and manager, scramble panel with 2D cube net, full-viewport focus mode while holding/inspecting/solving, last-solve quick penalties, stats summary, solve history with Ao5/Ao12 per solve, solve details dialog (penalty, notes, tags, delete with undo), touch/pen support, inspection audio cues, and screen-reader announcements.
44. Keyboard guard (`lib/timer/input.ts`): never intercepts inputs, text areas, open dialogs/menus or modifier shortcuts; keyboard-focused buttons keep Space. An e2e run exposed that a nav link left focused after client navigation swallowed Space; links activate with Enter, so they were removed from the guarded controls and a unit test was added.
45. Built Stats (stat tiles, progress chart with rolling averages, personal-best step chart, distribution histogram, averages table; every chart has a table view) and Settings (appearance, timer preferences, storage status, export/import). Charts load lazily. Ran the data-viz palette validator: the chosen three series pass CVD and normal-vision separation in light and dark; the one sub-3:1 light-mode color is backed by a legend and table view. Screenshot review found series colors changing between charts and faint axis labels; fixed so each average keeps one color everywhere.
46. The in-app dev preview could not start because the checkout lives under ~/Documents, which macOS privacy protection blocks for that process. Visual review used headless Playwright screenshots of the production build instead (`scripts/review-screenshots.mjs`).
47. Made the deployment base path configurable (`SOLVELAB_BASE_PATH`). Built with `/solvelab`, served it under that prefix and ran `scripts/check-base-path.mjs`: redirect, random-state scramble, a keyboard solve and client navigation all worked with zero failed requests.
48. Final validation: typecheck, ESLint and Prettier clean; 60 unit tests pass (8 files); production build passes; 25 Playwright tests pass, and 75/75 across three repeated runs with 4 workers. Details in docs/VALIDATION.md.

## UI overhaul (2026-09-13, branch `ui-overhaul`, not pushed)

The owner found the V1 interface bland and asked for a drastically more polished, interactive UI using 21st.dev resources, inspired by TAGDA Timer and csTimer.

49. Studied both references in the browser. TAGDA Timer: animated aurora shader, draggable glass panels, 3D cube preview, theme presets, shortcuts for everything. csTimer: dense and keyboard-first, live averages under the digits, LCD digits, sortable session table.
50. 21st.dev shows component code only to signed-in users; the owner signed in themselves in the preview browser. The registry then returned code, but free accounts are limited to two component downloads per day and the discovery requests used both. Components were therefore chosen on 21st.dev and taken from their authors' public registries (Aceternity Glowing Effect, Magic UI Border Beam/Magic Card/Confetti) or packages (NumberFlow, paper-design shaders). Motion-primitives' registry rate-limited requests, so its components were not used.
51. Replaced next-themes with a theme engine: six presets with CSS tokens, "Match system", a localStorage-backed external store read with useSyncExternalStore, and a boot script that applies the theme before first paint.
52. Added an animated WebGL mesh-gradient background that pauses during solves and for reduced motion.
53. Rebuilt the shell: floating pill navigation with a sliding indicator, mobile tab bar, command palette (⌘K) fed by a command registry, shortcuts help (?), and an Appearance sheet (T).
54. Rebuilt the timer page as a full-bleed canvas: scramble bar with history and custom scrambles, digit styles (Clean, LCD with ghost segments, Dot) and size, hold and inspection meters, live Ao5/Ao12, draggable glass panels for times (sortable, clear with undo), stats (NumberFlow, sparkline) and an interactive 3D cube, a PB border beam, toast and confetti, plus single-key shortcuts that never fire while typing or solving.
55. Moved the 3D cube's idle spin into motion values after noticing it re-rendered React every frame. NumberFlow renders in a shadow root and leaks a style tag into textContent, so plain-text copies now carry the values and test ids.
56. Owner request: mouse clicks must never control the timer. Pointer handling now ignores `pointerType === "mouse"` for both starting and stopping; touch and pen still work. Added an e2e test that holds and clicks the mouse on the timer.
57. The production build failed: the now-client `FeatureCard` received icon components from server pages. Made it a server component again, with only the glow effect as a client component.
58. The first e2e run had 15 failures and took 6.7 minutes. Headless Chromium renders WebGL on the CPU (SwiftShader), so the shader starved the main thread, and machines without GPU acceleration would see the same. The background now detects software renderers and shows one still frame, and renders at capped resolution. The last-solve bar's exit animation briefly rendered two copies of its controls, so it was removed.
59. A second run still had 12 failures after 8 minutes. System load averaged ~60: the checkout sits in iCloud-synced `~/Documents`, so builds, test artifacts and the dev server's file watching kept fseventsd, fileproviderd and cloudd busy. With the dev server stopped and Playwright on one worker with artifacts outside iCloud, 32/33 passed in 1.4 minutes. Recommendation: move the repository out of `~/Documents`.
60. The remaining failure was real: a panel dragged and then reloaded immediately lost its position, because motion fires `onDragEnd` on the next frame. Positions now also save on `pagehide` and on reset. The cube's idle spin repaints a blurred panel every frame, so it now runs only when a hardware GPU is present.
61. Screenshot review found the LCD ghost segments misaligned (different letter-spacing), Doto's decimal point drawn as a "+" (easy to confuse with a +2 penalty), and content showing through the phone tab bar. The ghost now shares the digits' metrics, the Dot style draws a square pixel for the decimal point, and the tab bar has a solid surface.
62. Final validation: typecheck, ESLint and Prettier clean; 66 unit tests pass (9 files); production build passes; 33 Playwright tests pass in 1.1 minutes (one worker).

## Handoff to Cursor (2026-09-13)

63. The owner is moving development to Cursor to save Claude credits. The dev server was restarted with `npm run dev` on `http://127.0.0.1:5173`, the same origin Cursor will use, so solves and settings recorded during the owner's review carry over. Browser smoke check: a random-state scramble loaded, a keyboard solve went armed → running → result and saved (then deleted), there were no console errors, and the phone tab bar is opaque.
64. Wrote `docs/HANDOFF.md` (state, branches, run/test, code map, gotchas, deployment notes, open questions), `docs/CURSOR_KICKOFF.md` (the message the owner pastes into Cursor) and `.cursor/rules/solvelab.mdc` (always-on rules). Added the key owner rules and a HANDOFF pointer to `AGENTS.md` and `README.md`. Documentation only; no code changes. `ui-overhaul` remains local and unpushed.

## Cursor session — result flash, local data, scramble types (2026-09-13)

65. Owner reported that a newly recorded time lagged: the previous solve flashed before the current one. Confirmed the cause: `getTimerDisplay` used the last IndexedDB solve for the stopped phase, the store notified React before `onComplete` ran, and `savingResult` was cleared before the live query caught up.
66. `TimerStore` now fires completion listeners before subscribers. Stopped/holding digits prefer the engine result until resting is the same raw time (so +2/DNF still show). The just-finished solve is inserted optimistically so the times list, last-solve bar and stats update in the same paint. NumberFlow digit-spin is disabled so averages don’t roll through the old value. Switching session or puzzle type resets the engine so another session’s time cannot linger.
67. Owner asked for on-device storage (“cookies and stuff”). No change: solves already live in IndexedDB `speedcubing-local`; appearance and panel layout in localStorage. Cookies are too small and would be sent to a server if the site is hosted. Nothing is uploaded.
68. Owner asked for scramble types besides 3×3. Added WCA events cubing.js can generate (2×2–7×7, OH, BLD, Pyraminx, Megaminx, Skewb, Square-1, Clock). Puzzle picker next to the session. Empty sessions are retargeted; sessions with solves switch to (or create) a session for that event. 3×3 net/3D preview only for 3×3 / OH / BLD. Fallback random-move generator stays 3×3-only so a failed 4×4 cannot silently become 25 random face turns.
69. Verification: 72 unit tests pass (was 66). Browser check at `http://127.0.0.1:5173/timer/`: after stop, 20 animation frames never showed the previous time; 2×2 switch produced an 11-move scramble, isolated 0/0 session, and no 3×3 preview. Did not stop the dev server for a full Playwright run so the owner’s review origin stays up. New e2e cases are in `tests/e2e/timer.spec.ts` for the next phase validation.

## Cursor session — CFOP subset scrambles (2026-09-13)

70. Owner asked for F2L, OLL and PLL scrambles (cross solved / F2L solved / OLL solved). These are not WCA event IDs, so they are generated as legal random cubie states with those pieces frozen, then solved with cubing.js (`experimentalSolve3x3x3IgnoringCenters`) and inverted. Added to the puzzle picker under a CFOP group. 3×3 preview stays on. Random-move fallback is not used for subsets. Vendor bundle now also exports `scrambleFrom333Pattern`.
71. Unit tests: cube + scramble files 19/19 with one worker (an earlier full run hit iCloud/dev-server timeouts on unrelated storage tests). Browser at `http://127.0.0.1:5173/timer/`: F2L produced a new 0/0 session and a scramble with the yellow D-cross visible on the net; PLL produced a short last-layer scramble (`F2 U' R2 B2 L2 D L2 B2 R2 U2 F2 U`). Did not stop the dev server for `validate` / Playwright.

## Cursor session — live subdomain (2026-09-13)

72. Owner chose `solvelab.bhargava-gumpula.com`. Skip WebGL mesh when there is no GPU / reduced motion / background off; lower shader resolution; lighter glass blur on touch. Production static server caches hashed assets (`SOLVELAB_CACHE=1`).
73. Stopped the local dev server, `npm run build` succeeded. Copied `out/` to the Pi at `~/Work/solvelab` and started pm2 `solvelab` on `127.0.0.1:4173` (200 OK locally). Cloudflare dashboard requires the owner's login, so the public hostname is not attached yet. Added a SolveLab card (demo URL) in the personal Website repo; that site is not rebuilt on the Pi yet.

## Cursor session — live e2e and clear-session fix (2026-09-13)

74. Owner confirmed `https://solvelab.bhargava-gumpula.com` is live (Cloudflare Pages-style HTML headers). All public routes returned HTTP 200. Browser check: random-state 3×3 scramble, Space-hold solve saved as 0.55 and survived reload, Stats showed that solve, Settings/Coach/Train/Algorithms/Learn rendered.

75. Playwright against production (`playwright.live.config.ts`, 1 worker, ~3.5 min): **35 passed / 1 failed**. The failure was `clears a session with undo`: after Clear, solve-count stayed `1/1` instead of `0/0`. Cause: the last solve is kept in React as `pendingSolve` until IndexedDB catches up; after Dexie is emptied that pending row looks new again and is merged back into the list.

76. Fix (local, not on Pages yet): drop pending on session clear, and drop it once it appears in IndexedDB so a later clear cannot resurrect it. Re-ran that spec against `npm run dev` on `:5173`: passed (14.7s). Live site still has the bug until the next `out/` upload. Owner asked to skip to V2 with a large change list; did not start spec V2.

## Cursor session — V2 slice: names, Bluetooth stub, Google gate (2026-09-13)

77. Owner scoped V2 now to: keep theme palettes, rename them, Bluetooth timer compatibility without Web Bluetooth, and real Google Sign-In gating Coach/Train/Learn. Layout uniqueness vs TAGDA is a later UI-only pass. V2.1 is AI/diagnostics/Instagram. Palettes unchanged; labels are Ion, Forge, Fjord, Sencha (default, id `matcha`), Graphite, Linen. Brand is V2.

78. Added `lib/timer/devices` (keyboard adapter + Bluetooth stub that rejects connect). Settings Hardware timer: Keyboard vs Bluetooth, Connect toasts that hardware is not wired. `timerInput` is a settings field filled by `normalizeSettings` (no Dexie version bump).

79. Firebase Auth client: `NEXT_PUBLIC_FIREBASE_*`. Without keys the build still runs; gated pages show Sign in with Google and “Accounts aren’t configured on this build.” Header account button, Settings Account section, AuthGate on Coach/Train/Learn. Timer/Stats/Algorithms stay public. No Firestore, no solve sync.

80. Disk filled during the change (APFS ~100%). Stopped `next dev` and deleted `.next` to free space. Unit tests for appearance, auth, timer devices, and storage: 22/22 passed. ESLint on new files clean. `tsc` hung writing incremental cache on a full disk; did not run the production build or Playwright.

81. Owner: Google sign-in reached the last Google screen then hung, and never returned to the timer. Popup flow cannot close that window reliably. Switched to `signInWithRedirect`, complete with `getRedirectResult` on boot, then `window.location.replace("/timer/")`. Auth uses `initializeAuth` with IndexedDB persistence and `browserPopupRedirectResolver`.

82. Owner: Google redirect completed, but the app stayed signed out. The Google tab finished on Firebase’s `__/auth/handler` page and never handed the session back (Chrome third-party storage). Sign-in now asks Google for a token on this origin (`signInWithCredential`), then falls back to a popup. The header updates in this tab; the client router goes to `/timer/` without a full reload. Auth unit tests 8/8. Did not run `validate` (shares `.next` with the running preview).

83. Owner: Google said the account wasn’t authorized. That was `origin_mismatch` — the OAuth web client was missing JavaScript origins `http://127.0.0.1:5173`, `http://localhost:5173`, and `https://solvelab.bhargava-gumpula.com`. Added those plus `/signed-in/` redirect URIs. Audience is already In production / External (not a test-user cap). Sign-in now uses a same-origin OIDC redirect to `/signed-in/` (no Firebase helper page, no GIS popup). Browser: Google account chooser for SolveLab, Allow, then `/timer/` with Account in the header and Coach unlocked (“Know what to practice next.”). Added Privacy and Terms at `/privacy/` and `/terms/` (solves stay in IndexedDB `speedcubing-local`; Google/Firebase only stores the account). Auth unit tests 8/8. Live Pages still needs a later `out/` deploy for those legal URLs.

## Cursor session — account cloud sync (2026-09-13)

84. Owner: an account is pointless if times vanish when browser data is cleared, and durable storage must not live on his laptop. Enabled Cloud Firestore `(default)` in `nam5` (United States) for `solvelab-1bb6e`, production-mode start, then published rules so only `request.auth.uid` can read/write `users/{uid}/**`. Client sync (`lib/sync`): on sign-in, merge IndexedDB with Firestore (union by id, last-write-wins, tombstones for deletes) and write the result back; Dexie hooks debounce a follow-up push after local edits. Working copy stays in the browser so the timer stays instant; nothing is written to the git checkout or the Pi. Privacy/Terms/Settings Account copy updated. Merge unit tests added. Did not run full `validate` (dev server still using `.next`). Live site still needs an `out/` deploy before production visitors get sync.

## Cursor session — 2.0 ship (2026-09-13)

85. Owner asked to ship this as **2.0**, show the version on the site, optimize without changing behavior, run e2e, push GitHub, and deploy live. Version is `2.0` in `lib/config/brand.ts`, footer (`SolveLab 2.0`), Settings About, and `/overview/`. Overview lists what 2.0 includes and that 2.1 is UI and 2.2/3.0 is a local coach. Sync now diffs against the last pushed snapshot: a new solve is one Firestore write instead of rewriting every document; sign-in still merges. No timer/UI redesign. `npm run validate` passed (99 unit tests, production build includes `/overview`). Playwright 39 tests: two auth locators were ambiguous (header + page Sign in; Overview subtitle + heading); scoped them. Full e2e then green. Pushed `ui-overhaul` and fast-forwarded `main` (release `d60c1db`, log follow-up `a0480f9`); tag `v2.0.0` on GitHub. Pi `~/Work/solvelab` updated via `deploy-pi.sh`. Uploaded `out/` to Cloudflare Pages project `solvelab` so `https://solvelab.bhargava-gumpula.com` serves 2.0 (`/overview/`, `/privacy/`, `/terms/`, `/signed-in/` return 200; footer shows SolveLab 2.0). Temporary deploy API token was revoked after upload.

## Cursor session — resizable timer panels (2026-09-14)

86. Owner asked for timer side panels to scale with screen size, each be user-resizable (with a minimum), and for those sizes to sync with the signed-in account. Added `panelLayout` on `UserSettings` (IndexedDB + Firestore with other preferences). Defaults fill the leftover columns (Times stretches, stats/cube split the right side); saved sizes are scales of that slot so they still follow the screen. Drag + SE resize handles on desktop; Appearance “Reset panel layout” clears sizes and offsets. Legacy pixel sizes from the first cut are ignored. Privacy copy updated. Unit tests for panel layout helpers pass; e2e drag persistence now reads IndexedDB settings instead of localStorage.

87. Owner rejected the resize work: panels were clipped (“cut out spots”) and crowding was worse. Removed user resizing and `panelLayout` persistence. Restored content-sized stats/cube (no overflow clipping) and a CSS `clamp()` column width so the layout follows the screen without looking cramped. Times still fills the left column and scrolls its list. Drag positions stay in `solvelab.panels.v2` (bumped so leftover offsets from the resize experiment don’t overlap).

88. Owner: still didn’t fit; cube was cut off and they didn’t want to scroll. Switched the desktop timer to a CSS grid so Times / timer / stats+cube share leftover height after a smaller scramble bar. Compacted scramble type, stats padding, sparkline, and cube preview so the whole timer stays on screen. Dropped extra desktop main padding that caused a 20px page scroll. Panel drag key is `solvelab.panels.v3`.

89. Screenshot on a 1280×674 pane still showed the cube covering stats: leftover `translateY(~-158px)` from `solvelab.panels.v3`. Drag key is now `solvelab.panels.v4` (large saved offsets ignored; no `pagehide` rewrite). Shrunk the scramble bar to one ~38px row, compacted stats on short viewports (drop Ao100), and gave the cube the leftover column so Times/stats/cube all sit on screen with `pageScroll: 0`. Confirmed with a live screenshot: single-line scramble, full stats, 2D net and 3D cube fully visible, nothing chopped.

90. Owner: scramble bar a bit bigger (midpoint of the 38px and 64px versions — now ~50px, `text-base`, not `lg:text-lg`); crop extra glass on Times and scramble preview without shrinking the cube or the times list. Times is content-height (`max-h-full`) instead of stretching the column; cube preview is `shrink-0` hugging the net/3D cube instead of `flex-1` empty space.

91. Owner: Times and stats sat too high, scramble preview too low. First pass centered the side columns as a stack (bad). Restored the intended layout from their screenshot: Times and stats stay at the top, scramble preview stays at the bottom (`mt-auto`), with column padding so the preview is not flush to the bottom edge.

92. Owner said the redo still didn’t match their screenshot. Matched it at 1024×721: side columns use `justify-between` with `py-10` so Times/stats sit below the scramble with room above, the preview sits off the bottom with room below, and the middle gap opens up. Empty short-viewport stats drops the unused trend block so the gap can breathe; cube preview keeps internal padding like the reference.

93. Owner: no hardcoded insets — evenly space panels from edges and each other, fluid with screen size; scramble bar only a tad wider. Desktop uses equal `1fr` spacer rows (`1fr auto 1fr auto 1fr`) shared across columns so top / between / bottom gaps match and Times lines up with stats; center column uses `min-h-0 overflow-hidden` so it doesn’t skew the spacers. Scramble `max-w` bumped from `5xl` (64rem) to `67rem`.

94. Owner: ship **2.1** — add optional 3 decimal places (Appearance), mark 2.1 complete, plan 2.2 as Bluetooth + initial AI coach/training/lessons, run e2e, push and deploy. `timeDecimals` is an appearance preference (2|3, default 2); `formatTime` / timer display / stats / lists honor it. Brand version `2.1`. Overview and planned copy updated for Interface / 2.2 hardware+coach. Auth e2e expected “SolveLab 2.0”; updated to 2.1. Full Playwright: **40 passed**.

95. Owner: panel drag positions and inspection should restore when signed in elsewhere. Inspection (and other `UserSettings`) already sync via Firestore `settings/preferences`. Added `panelOffsets` to `UserSettings` so desktop panel drags write IndexedDB (and therefore the account) while keeping `solvelab.panels.v4` as a local cache/migrate path. Privacy copy updated.

96. Owner: scramble box a little bigger. Bumped bar `max-w` `67rem` → `72rem`, padding, and scramble type one step (`text-base` / `sm:text-lg`; long scrambles `text-xs` / `sm:text-sm`). Pushed `6c41f3c` to `ui-overhaul`/`main`; Pi + Cloudflare Pages redeployed; temporary Pages deploy token revoked.

97. Owner: overnight build of **2.2** (asleep until 6am; no interactive permissions). Shipped brand `2.2`. Bluetooth timer path: Web Bluetooth best-effort + Stackmat simulator pads wired into the timer store when `timerInput === "bluetooth"`. Rule-based on-device coach (`lib/coach`) with baseline, skill scoring, diagnosis, training plans, retest helpers, and template narratives. Coach/Train/Learn UIs use real flows (Train arms `activeExerciseId` onto solves; Learn has full lesson content + local progress). Coach repositories for skill profiles / plans / diagnostic runs. Expanded exercises. Tiny local logistic-regression weight trainer under `ml/` with unit test. Overview/HANDOFF/README updated; planned slice is 2.3.

98. Owner: 20-param logistic stub is inadequate — wants ~2k–20k params and real accuracy. Replaced with an **18,408-param** MLP (`40→128→96→8`), causal synthetic trainer, holdout test **~84%** (8-way chance 12.5%). Wired into `analyzeSolves` to blend with the rule engine when diagnostics exist.

99. Owner: ship **3.0** as diagnostic-only. Feature flags hide Train/Learn; Algorithms stays browsable with planned copy **3.1–3.2**. Coach is goal → diagnostic → slow/average/fast stage tags (live Dexie + shared pace window). Train/Learn routes show ComingSoon. Brand/package **3.0.0**. Unit 135 + Playwright **46** green. Standalone `tsc` / Next build-time typecheck hang idle under this ~/Documents checkout — build uses `typescript.ignoreBuildErrors` (lint + vitest + e2e still gate). Pushed `ui-overhaul`, fast-forwarded `main`, tag `v3.0.0`, Pi + Cloudflare Pages deploy.

## Claude Code session — fresh checkout off iCloud (2026-09-18)

100. The `~/Documents` checkout from earlier sessions isn't on this machine, so `main` (`174279e`) was cloned fresh from GitHub into a folder outside iCloud, with Node 26.9.0 / npm 11.19.1. `npm ci` added 696 packages; npm notes that the `esbuild`, `protobufjs`, `unrs-resolver` and `@firebase/util` install scripts aren't covered by `allowScripts` yet (they still ran). Lint, Prettier check, Vitest **135/135** (18 files) and `npm run build` pass; Playwright `--workers=1` **46/46** (1.2 min). Off iCloud, standalone `tsc --noEmit` finishes in about a second instead of hanging, and reports one error that `typescript.ignoreBuildErrors` has hidden since 3.0: `hooks/use-coach-pace.ts:18` gives `useLiveQuery` a querier returning `PromiseExtended<DiagnosticRun[]> | undefined`, which Dexie's typings reject. Proposed fix, awaiting the owner: make that querier `async` like the ones in `hooks/use-local-data.ts`, then drop `ignoreBuildErrors` from `next.config.ts`.

101. Owner approved the fix. The `diagnosticRuns` querier in `useCoachPace` is now `async`, like the live-query hooks in `hooks/use-local-data.ts`, and `typescript.ignoreBuildErrors` is gone, so `next build` type-checks again (Next 16.3 runs the project's `tsc`, ~2s). `npm run validate` passes: typecheck clean, lint, Prettier, Vitest 135/135, build. Playwright `--workers=1`: 45/46 on the first run, 46/46 on the second. The failure, `data.spec.ts` › exports a backup and restores it into a fresh browser profile, is pre-existing: the restored last solve shows `0.51` with no `+`. Run alone with `--repeat-each=10` it fails 9/10 with this change and 10/10 on the original code, while full-suite runs usually pass. The exported backups record `penalty: "none"` for both solves, so the +2 clicked on the last solve isn't saved before the test navigates to Settings. Not fixed here; still to determine whether the test races the save or the app drops the penalty.

102. Followed up the flaky backup e2e. The source page's trace shows no "Couldn’t update the penalty" toast, and with a temporary `await expect(display(page)).toHaveText(/\+$/)` after the +2 click (before `page.goto("/settings/")`), the test passed 10/10 in isolation; the test file was then restored. So the app saves the penalty; the test navigates away before the IndexedDB write commits, which a person can't do. Proposed fix, awaiting the owner: add that wait to `tests/e2e/data.spec.ts`.

## Claude Code session — 3.1 plan, phase 1: save and sync everything (2026-09-18)

103. Owner asked for a much larger coach: a full solve profile (every aspect with tag, goal and current average), an AI that picks the next test, a guided conversational Coach with a sandbox where accidental attempts can be deleted, training packs with researched lessons, a filled-in algorithm bank, real user data used to train the model (on by default, opt-out, including signed-out users), and every customizable choice saved and synced. Agreed a five-phase plan with a review stop after each: (1) save and sync everything, (2) tests, sandbox and Solve profile plus data contribution, (3) AI test planner and guided coach plus real-data retraining, (4) algorithm bank (2-look OLL/PLL, OLL, PLL, F2L, COLL, WV), (5) training packs. A BETA chat with the owner's own AI provider is a later phase. Owner actions noted: enable Firebase Anonymous sign-in, deploy updated Firestore rules, run data exports with their own admin credentials.

104. Phase 1. Account sync now works from a registry of synced tables (`lib/sync/collections.ts`): sessions, solves, diagnostic runs, training plans, skill scores, algorithm progress and attempts, and lesson progress, each at `users/{uid}/{table}/{key}`, merged last-write-wins with tombstones keyed by table ("session"/"solve" kinds kept for 2.x data). Coach writes now stamp `updatedAt`; older runs fall back to `completedAt` so a finished run beats an unfinished copy. Appearance moved into the settings record (still mirrored to localStorage for the no-flash boot script) through `components/appearance/appearance-sync.tsx`; a device's pre-3.1 appearance is adopted once without bumping the settings edit time, so a newer account copy still wins. Stats range, analyzed session and times sort persist in `settings.view` (merged per field in one transaction so quick changes don't overwrite each other). Dexie v3 adds `lessonProgress`; the old `solvelab.lessonProgress.v1` localStorage copy is imported once and removed. Backup format v2 carries every synced table plus appearance and view; v1 still imports. Privacy page and DESIGN.md updated (effective date September 18, 2026). Fixed the flaky backup e2e by waiting for the saved "+" before navigating (10/10 in isolation).

105. Results: `npm run validate` passes (typecheck, lint, Prettier, Vitest **153/153** in 18 files, build with type checking). Playwright `--workers=1` **49/49** (1.1 min), including three new persistence tests (appearance returns from settings after the local copy is removed; stats range, analyzed session and times sort survive a reload; the backup carries appearance and view). Real Firestore sync still needs a signed-in check by the owner.

## Claude Code session — 3.1 plan, phase 2: skill tests, solve profile, coach training data (2026-09-18)

106. Owner: real people will use this, so the screens should say what a person wants to do ("Start cross test"), never "open sandbox" or "diagnostic", and the learning flow should be simple and interactive. Phase 2 built on that. - **Tests** (`data/exercises`, `TEST_ORDER`): the existing stage tests plus unlimited-inspection cross, cross + F2L, single pair (new `333ls` scramble: cross and three pairs solved), slow-turning F2L, last pair + OLL, and a turning-speed test (R U R' U' × 6, no scramble). Each has a plain name, what it shows, its inspection mode and attempt count. The cross tests use 15-second inspection. - **Solve profile** (`lib/coach/aspects.ts`, `lib/coach/profile.ts`): 15 aspects, from cross, cross planning and cross → F2L through pair speed, lookahead, F2L → OLL, OLL/PLL and their algorithm knowledge, OLL → PLL, full solve, consistency and turning speed. Joins are "combined test minus its parts" with a likely range. Algorithm knowledge is the share of attempts over 1.5× the median. Goals per level come from `data/milestones/aspect-targets.ts`, with sources cited; the stage bars now derive from it. Each finished test saves a synced profile snapshot (Dexie v4, `profileSnapshots`) so rows can show a trend. - **Test page** (`/coach/tests/<id>/`): numbered steps, the goal for the whole test, progress, the scramble (or algorithm) and timer. Attempts can be deleted by the ✕ or Backspace, with Undo. "Save and exit" resumes later. "Finish now" is available from 3 attempts, and the test finishes by itself at the target. Results show the average against the goal, a card per aspect the test feeds, "Next: … test", the profile and retake. Attempts stay in `diagnosticRuns` and never touch timer solves. The old `/coach/diagnostic/…` links redirect, and the old stage sandboxes are gone. - **Stats → Solve profile** (`/stats/profile/`): pick a goal (a suggestion comes from the timer average), then an "Up next" card, grouped rows (value, trend, goal, tag, and "Start / Continue / Retake … test" or "Solve on the timer"), expandable detail (verdict, how it's measured, the parts, likely range), and every test with its status. - **Coach page**: until the phase 3 conversation, it shows goal chips, one message with the next test, and "What to work on first" with the three slowest aspects and tips. - **Coach training data** (`lib/training-data/`): after a finished test, the app shares the test id, attempt times, inspection mode, goal, UTC day, app version and a timer summary (count, average, CV). It never shares identity, notes, tags, scrambles or raw solves. Data goes to `trainingContributions/{uid}/runs/{runId}`, and signed out that uid is an anonymous Firebase id, which the app treats as signed out. Google sign-in links the anonymous id; if the account already exists, the anonymous results are deleted and shared again under the account. On by default (`contributeTrainingData`, synced). A one-time notice offers "Got it" / "Don't share", and Settings → Your data has "Help improve the coach". Turning it off deletes what was shared, retrying later if offline. `firestore.rules` checks the exact shape. The privacy policy ("How your solve data is used") and terms say training the coach is the only other use of solve data.

107. Checked by hand in the browser at desktop and phone widths: goal → cross test → attempts (with 15 s inspection) → delete + Undo → reload resumes → auto-finish at 10 → results → Solve profile. Two fixes came from that pass: "Picking up where you left off" showed after the first attempt of a fresh test, and profile rows didn't line up because the row trigger sized to its content. On the dev build, sharing reached Firebase and got `auth/admin-restricted-operation`, because Anonymous sign-in isn't enabled on the project yet. The run stays queued and shares once the owner enables it and deploys the rules.

108. Results: `npm run validate` passes (typecheck, lint, Prettier, Vitest **180/180** in 20 files, build). Playwright `--workers=1` **54/54** (1.4 min). Every e2e test now runs through `tests/e2e/fixtures.ts`, which blocks and records Firebase Auth/Firestore traffic, so tests never write to the real project. New specs: `skill-tests.spec.ts` (goal → test → delete/undo → results → profile; save and exit then continue; turning speed; tests don't touch timer averages; old links redirect) and `coach-training.spec.ts` (notice shows once and a finished test is shared; turning sharing off is saved and sends nothing; "Don't share"). `scripts/review-screenshots.mjs` now also captures the coach, the solve profile (desktop and phone), a test and its results.

109. Owner: any task that needs them but isn't a matter of opinion should go through the aside-browser skill. With their approval (and a permission rule for `aside exec`), Aside turned on the **Anonymous** provider in Firebase Authentication (Google unchanged) and published `firestore.rules` on project `solvelab-1bb6e`. It first checked that the live rules were only the `users/{userId}` block; the published text matches the repo line for line. Checked end to end from the dev server: a finished test uploaded under a new anonymous id (the app still shows "Not signed in."), and turning **Help improve the coach** off deleted it from Firestore ("Sharing is off, and what you shared has been deleted."). The test data was then cleared from the preview browser so the owner starts fresh.

110. Owner testing feedback on phase 2: (a) the unlimited-inspection results made no sense ("how does it know my cross planning and what does cross planning even mean?"); (b) "Up next" kept asking for a retake of the single pair test forever; (c) add an optional quick daily check with two attempts of each part. - **Clearer joins.** "Cross planning" is now **Inspection planning** ("time your cross loses because 15 seconds isn't enough to plan all of it"), and its test is the **Unlimited-inspection cross test**. Every number now shows its working (`aspectMath` in `lib/coach/profile-format.ts`) on result cards and expanded profile rows, e.g. "Cross test 1.67 s − Unlimited-inspection cross test 1.52 s = 0.15 s", "F2L test 6.70 s − 4 × Single pair test 1.45 s = 0.90 s", "24 turns ÷ 2.95 s = 8.1 turns/s". A comparison that's missing a test says which one. - **No retake loop.** The ten **core tests** (`CORE_TESTS`) make up the profile. Slow-turning F2L and cross + first pair are now **Extra tests**, optional and listed separately. "Up next" offers only an unfinished test or one not yet done. After the last one, the results page, profile and Coach all say "Your solve profile is complete." Previously, once everything was taken it fell back to the slowest aspect's test, which was a retake. - **Daily check** (`/coach/daily/`, `lib/coach/daily-check.ts`, `components/tests/daily-check.tsx`): two attempts of each core test, back to back (about 5 minutes). It moves on by itself after two attempts and keeps your last time on the display. Deleting an attempt brings that test back, tests can be skipped, and "Save and exit" resumes the same day. Results show Today vs Profile vs Change (better / worse / about the same, 2% band or 50 ms for joins), today's tag against the goal, a small trend line across the last 14 checks, and a streak. A check never changes the solve profile. Checks are a new synced table (`dailyChecks`, Dexie v5, backup, account sync). An opt-in **Remind me each day** (`dailyCheckReminder`, synced) puts a dot on Coach in the menu until that day's check is done. The Coach page and the completed profile show a daily-check card. Daily checks aren't shared for coach training.

111. Checked in the browser: the profile read "9 of 10 tests done" with the turning-speed test up next. Finishing it showed "Your solve profile is complete" with no next test. The working appeared on every join. A full daily check (20 attempts, with inspection on the cross tests) moved through all ten tests and ended "Better than your profile on 7 of 11 parts" with a 1-day streak, and the menu dot cleared. Long row buttons ("Retake unlimited-inspection cross test") now wrap instead of overflowing.

112. Results: `npm run validate` passes (Vitest **190/190** in 21 files: completion and continue-first, the working for each kind, daily-check order/skip/streak/comparison/history, Dexie v4→v5, daily-check repository). Playwright `--workers=1` **56/56**. New specs: finishing the last core test completes the profile (no retake loop), with the working shown; and a daily check with the reminder dot, delete-and-redo, skips, results and a reload. `keyboardSolve` takes `{ moveOn: true }` for the last attempt of a test, since the timer leaves the screen when it stops. Also fixed a skip race in `coach-training.spec.ts`: its setup counted the sharing switch before Settings had loaded and sometimes skipped two tests. It now waits for either the switch or the "not configured" text (3/3 repeats pass).

113. Owner: the daily check looked odd in its own box on Coach, so it moved into the main Coach box. It's now a button beside "See your solve profile" ("Start daily check", "Continue daily check" or "See today's daily check", with the streak). The card stays on the completed Solve profile, where it already sits inside the summary box. Also fixed a flaky unit test ("saves profile snapshots in order"): two snapshots saved in the same millisecond had no fixed order. Real snapshots come one per finished test, so only the test changed. It now passes 8/8 repeats. `npm run validate` passes (Vitest 190/190) and Playwright `--workers=1` is 56/56.

114. Owner: make the slow / average / fast chips in the Solve profile summary box "a tad bit bigger" so the box has less empty space. Only those three changed: the count is `text-xl`, the tag `text-sm` with more padding, and the chips are 46 px tall instead of about 30. Tags in the aspect rows, cards and daily check are unchanged. Checked in the browser.

## Claude Code session — 3.1 release: solve profile (2026-09-19)

115. Owner signed off on phase 2 ("this is actually perfect") and asked for one more fix, an answer, docs, and a release. - **Empty box during a solve.** In a test or the daily check, starting the timer faded the scramble but left an empty frosted box. The cause: the timer card's `backdrop-filter` makes it the containing block for the "full-screen" (`position: fixed`) timer surface, so the surface stayed trapped in the card. The card now has `data-focus-shell`, which drops its background, border, shadow and blur while a solve is timed (`app/globals.css`), so the timer fills the screen like on the Timer page. The footer links also hide in focus mode now. Checked in the browser: the surface is 1024×768 at 0,0 and only the time shows. - **"Is the local AI used, and was it when I tested?"** No, on both counts. Every number, tag and "Up next" comes from plain formulas (`lib/coach/profile.ts`) and the goal table. The small 3.0 model (`ml/coach-mlp.json`, through `lib/coach/index.ts` `analyzeSolves`) is only reachable from `components/train/train-dashboard.tsx`, which no route renders. Phase 3 builds the real planner/diagnoser. What does happen today is collecting shared test results to train it. - **Version 3.1 "Solve profile".** Package 3.1.0, brand 3.1, Overview page and README updated. The roadmap moved to 3.2 AI coach, 3.3 algorithm bank, 3.4 training packs and lessons, so "Train"/"Learn" say 3.4 and Algorithms says 3.3. The e2e expectations were updated to match. - **Docs for starting fresh chats.** The owner asked whether a new chat keeps context. It doesn't: a new chat gets only `CLAUDE.md`/`AGENTS.md` and its memory notes. So `docs/OVERVIEW.md` was rewritten as the product summary (idea, principles, features, how the coach thinks, tech, plan, history), and `docs/NEXT_STEPS.md` was added with the detailed Phase 3–6 plan, open items, and a kickoff message for a new chat. `AGENTS.md` now points at those, sends non-opinion owner tasks through Aside, and says deploy only when asked. A new `CLAUDE.md` imports `AGENTS.md` so Claude Code loads it automatically. HANDOFF, the README and this log are updated.

**What we did in this session, in short (entries 100–115):** cloned the repo to a fresh Mac outside iCloud and set up Node 26, Xcode tools and `<bits/stdc++.h>`. Fixed the hidden type error and re-enabled build type checking. Agreed a five-phase plan for a real coach. Phase 1: every choice saved and synced (registry sync, appearance and view in settings, Dexie v3, backup v2). Phase 2: ten skill tests with deletable attempts, the 15-part Solve profile with goals per level, and coach training data (anonymous Firebase ids, Firestore rules, privacy and terms), enabled on Firebase through Aside. Then the owner's feedback: plain-language joins with visible working, no retake loop, a daily check, the daily check inside the Coach box, bigger summary chips, and no empty box during a solve. Released as 3.1.

116. Owner: make the repository public, clean out files that don't belong (like the Cursor rules), and git-ignore personal data. Before switching visibility, scanned the whole git history for secrets (Google/Firebase API keys, GitHub/OpenAI/AWS/Slack tokens, private keys, service-account JSON, env files) and found none. The Firebase web config has only ever lived in the git-ignored `.env.local`. Then ran `gh repo edit --visibility public`; <https://github.com/bhargava-gumpula/solvelab> is now public. - **Removed:** `.cursor/rules/solvelab.mdc` (it duplicated `AGENTS.md`, which `CLAUDE.md` now loads), `docs/CURSOR_KICKOFF.md` (replaced by HANDOFF and NEXT_STEPS), and `scripts/solvelab.pm2.cjs` (unused, and it had the Pi's home paths). - **Kept:** `vendor/` (imported by `app/globals.css`) and `playwright.live.config.ts` / `playwright.dev.config.ts` (run the e2e suite against the live site or the dev server). - **Personal data:** `scripts/deploy-pi.sh` no longer hard-codes the Pi's username, LAN IP and paths. It reads them from a git-ignored `.env.deploy`, created locally with the old values. - **`.gitignore`** is reorganized, with a "Personal data and secrets" section: env files, keys and service-account JSON, training-data exports (`/ml/exports/`, `/ml/data/`), solve backups, Firebase CLI state, and AI/IDE tool folders (`.claude/`, `.cursor/`, `.aside/`, …). No tracked file is affected. - **Note:** older commits still contain the Pi's username and private LAN IP (10.0.0.x, not reachable from the internet). Removing them would mean rewriting history and force-pushing; that's the owner's call.

117. **Released 3.1.** `ui-overhaul` was pushed and `main` fast-forwarded to `898444b`, tagged `v3.1.0`. Rebuilt `out/` (410 files, 9.5 MB; the zip is 2.5 MB). Aside uploaded the zip as a new **Production** deployment of Cloudflare Pages project `solvelab` (deployment `ac2fe1a9`, 1:08 AM PT, status success, with `solvelab.bhargava-gumpula.com` attached). Aside signed in to Cloudflare with the owner's existing Google session. No API tokens were created or seen, and no other settings changed. Verified live: `/overview/` shows "SolveLab 3.1", and `/coach/daily/`, `/stats/profile/` and `/coach/tests/…` return 200. `playwright.live.config.ts` against the live site passed 4/4 (public pages; goal → cross test → delete/undo → results → profile; daily check; old links redirect). The Pi fallback wasn't updated because this Mac has no SSH key for it.

118. The owner asked why SolveLab is on Cloudflare Pages while their main website runs on the Raspberry Pi. It's historical: SolveLab was first copied to the Pi (entry 73), but the public subdomain went live on a Pages project (entry 74) and every release since was uploaded there. The Pi copy stayed behind, stale. Pages suits a static site: it's served worldwide and stays up when the Pi or home internet is down. The main website (GitHub `Website`) needs a server for its API routes: Stripe checkout and webhooks, contact email over SMTP, Google Calendar slots, and the testimonials admin login. Owner decision: **keep Pages and retire the Pi copy.** Removed `scripts/deploy-pi.sh` and the local `.env.deploy`, and updated HANDOFF §8, ARCHITECTURE, OVERVIEW and NEXT_STEPS. Stopping the Pi's pm2 process needs the owner on the home Wi-Fi: this Mac got "No route to host" for the Pi's LAN address, and the Pi only accepts SSH from the LAN.

## Claude Code session — Phase 3: the AI coach (2026-09-19)

119. Owner: "start phase 3". Built the AI test planner and the conversational coach (release 3.2), following docs/NEXT_STEPS.md. - **Goals for any average:** `aspectTargetsForTime` interpolates the level table on a log scale, so the simulator can create solvers at any speed. - **Simulator** (`ml/sim.ts`): cubers from about 9 s to 100 s, each a typical solver for their average with person-to-person variation, plus 0–3 weaknesses. The 13 kinds: cross execution, inspection planning, cross → F2L, pair fluency, lookahead, F2L → OLL, OLL/PLL execution, OLL/PLL knowledge (unknown cases take about 2.2× as long), slow turning and inconsistency. Attempts have lognormal noise, lockups, and early finishes. A part is "really slow" when its noise-free value is rated slow against the goal the cuber picked, by the same rules as the profile. - **Model** (`lib/coach/ai/`): a pure-TypeScript network (ReLU layers, Adam; sigmoid, softmax or linear outputs). Its 82 inputs come from the same formulas as the Solve profile. The diagnoser (82 → 64 → 64 → 13) is trained on 180,000 partial snapshots of 30,000 cubers, with a cut-off per part tuned for F1. Training takes about a minute (`npm run ml:train`). - **Guards** (`guards.ts`): comparison tests only after their parts, and at least 4 tests before the coach may stop. - **Coach engine** (`lib/coach/coach-engine.ts`): the conversation as a pure function of the thread and the person's data. It records finished tests, asks for the next one, or sums up. Tests from the last 14 days count, so someone with recent tests gets a summary at once. "Start over" measures from scratch; "Retest my weak spots" retakes each weak part's deciding test. Conversations are a synced table (Dexie v6 `coachThreads`, in backups and account sync). Steps are added inside a transaction, so two tabs can't add the same step. - **Coach page** (`components/coach/coach-thread.tsx`): coach and "you" bubbles, goal chips, and Start / Skip on the current request. The summary card has the top 3 weaknesses (the model's confidence, why it usually happens, three tips, a drill, sources), then "Also slower than your goal", "Worth checking" (untested parts the model suspects, with a button to take the test), and every other part with tips. Earlier summaries are kept too. Test results show "Back to your coach" when the coach asked for that test. - **Tips** (`data/coach/tips.ts`): all 15 parts, researched from J Perm, CubeSkills, SpeedSolving (wiki and threads), Cubefreak, Badmephisto, Sarah's cubing site, SpeedCubeDB, Jayden McNeill, Cubelelo and CuberPal. Written in our own words, hedged, with links.

120. What testing showed, and what changed because of it. - **Unrealistic data:** hand-seeded data (a 2.18 s F2L for Sub 20) made the model call an _untested_ part "very likely a weakness". Log ratios are now held between −1 and 1.5, and untested suspects never go under "Work on these first"; they go in "Worth checking" instead. - **The OLL/PLL algorithm share failed when most cases are slow:** the median itself is then a slow case, so "attempts over 1.5× the median" read 0%. It's now measured against the quick attempts (lower quartile), in the profile, the model and the texts. - **The summary contradicted itself** when the model and the raw number disagreed. `summaryStatus` now makes the badge follow the model and adds a short note. A number over the goal that the model thinks is noise shows "Average" with "likely mostly noise, retake to be sure". - **Wording:** "the f2l test" and "cases … is" were fixed with `testPhrase` and plural verbs. - **The trained planner didn't work.** It was a second network (82 → 64 → 64 → 10, linear) trained in hindsight to predict how much each test would lower the diagnoser's error. At equal test counts it was no better than a random order: 0.782 vs 0.790 at 6 tests, 0.796 vs 0.808 at 7. Loosening the stop rule showed the trade-off, but not a better planner. It was replaced by an **uncertainty planner**: ask for the test whose parts the diagnoser is least sure about (summed entropy), and stop when that drops below a threshold. It beats random at every test count (0.823 vs 0.812 at 7.4 tests). The threshold (0.3) is the fewest tests within 0.015 F1 of using every test. - **Final benchmark** (3,000 fresh simulated cubers, F1 for finding real weaknesses): rules with all 10 tests 0.774; diagnoser with all 10 tests 0.833; **coach 0.820 with 7.4 tests on average** (random order at the same count 0.810). About a fifth of cubers are clear after 4 tests and a quarter need all 10. The weights are 186 KB (71 KB gzipped), loaded only on the Coach page. The rules run the same flow if they fail to load. - **Checked in the browser:** a fresh start asks for a goal, then the PLL test first (two uncertain parts at once). A finished test gets a results message and the next request; Skip works; the conversation survives reloads. A realistic simulated cuber (56 s, Sub 45 goal, lookahead + PLL-knowledge weaknesses) got F2L, PLL and lookahead as its top three, all real gaps. - **The old 3.0 model was removed:** `ml/mlp.ts`, `ml/coach-mlp.json`, its training scripts, `lib/coach/ml-features.ts`, `tests/unit/ml-weights.test.ts` and the unused `stage-pace-list.tsx`. `analyzeSolves` (still used by the Train preview) is now rules-only. - **Old Coach page:** `components/coach/coach-dashboard.tsx` is deleted; the conversation replaces it.

121. **Real data pipeline.** - `npm run ml:export`: the owner's own service-account key, read through `GOOGLE_APPLICATION_CREDENTIALS`, never read by the agent. It reads every `trainingContributions/*/runs/*`, keeps only records that pass the same checks as the Firestore rules, replaces account ids with random per-export ids, drops the run ids, and writes `ml/exports/…` (git-ignored). The logic lives in `ml/export.ts`, with unit tests showing no uid, run id or extra field leaks. - `npm run ml:calibrate -- <export>`: compares real time-vs-goal and spread with the simulator per goal band and test, flags differences over 15%, and counts retests. Smoke-tested on 40 simulated cubers exported as a fake file. - `npm run ml:sample`: prints one simulated cuber, for demos. - `firebase-admin` was added as a dev dependency; it isn't in the site bundle. - `ml/README.md` has the model card, benchmark and retraining runbook. - The Privacy Policy now lists coach conversations and summaries among synced data.

122. Review pass on the screenshots. Opening Coach with an existing summary jumped to the bottom; it now scrolls only when new messages arrive. Timer-based weak parts (consistency, full solve) have no model probability and were ranked first; they now rank as a moderate call (0.6), after the model's confident weaknesses. The screenshot script now also captures `coach-summary` (desktop and phone), with all ten core tests seeded. Results: `npm run validate` passes and Vitest is 208/208. Playwright was 57/58 on the full run; the failure was an old check for the removed Coach headline, now updated to expect a new request (the imported tests are over two weeks old). The coach and skill-test specs then passed 10/10, and again after the scroll fix.

123. Owner, after trying the conversation: make it feel like there is actually an AI there. - **A face.** New `components/coach/coach-avatar.tsx`: a cube with eyes, drawn from the theme tokens, beside every coach message and in a new header that names the coach (**Cube Coach**) and shows what it is doing — "AI coach · runs in your browser", "Typing…", or "Standard test order" if the model didn't load. It blinks about every 6 seconds, and twice as often while it writes. - **Typing.** Messages are revealed one at a time, with three bouncing dots standing in while the coach "writes": 700 ms for the greeting, 900 ms for a short note, 1.1 s for a test request, 1.5 s for results and 2.3 s for the summary, which also captions the dots "Working out your summary". Your own replies (goal, skip) appear at once with no dots, since you just sent them. A conversation you reopen is shown in full — it has already been said — while a new one, including after "Start over", is typed out. A backlog of several messages is written at half the delay so a chain doesn't drag, and the whole thing is skipped when the browser asks for reduced motion. - **No typing back yet.** A disabled chat box under the thread says so and links to Settings. - **Settings → AI coach:** explains that the built-in coach runs on the device and that times are never sent to produce its advice, then offers "Connect your own AI" with a "Coming later" badge (the Phase 6 BETA chat).

124. Owner, after signing out: their coach conversations were still there, which is wrong, and the parts of the site that hold a person's own data should need an account. - **Signing out now clears this browser.** It asks first ("Your times, coach conversations and solve profile stay on your Google account and come back when you sign in"), then signs out, **drops** the local database and removes every `solvelab.` key except the appearance one, and reloads at the timer. The database is deleted rather than emptied on purpose: emptying it would run the sync hooks, which record a deletion per row and would push those to the account on the next sign-in, wiping the person's times in the cloud (`lib/storage/reset.ts`, `lib/auth/sign-out.ts`, `components/auth/sign-out-dialog.tsx`, used by the header menu, the command palette and Settings). A delete blocked by another tab gives up after 4 seconds and the reload finishes the job. - **Coach, Stats, Train and Learn need an account** (the owner's choice; the timer and Algorithms stay open so the site can still be tried). `lib/auth/access.ts` holds the rule and `components/auth/require-account.tsx` the sign-in card, wired into each area's layout, so nested pages such as `/coach/tests/…` and `/stats/profile/` are locked too. While the account is still being checked it shows a placeholder, so the card never flashes at someone who is signed in. **A build with no Firebase config locks nothing**, since there would be nothing to sign in to. - **Quieter sync.** A sync that fails because the device is offline no longer shows an error toast (`isOfflineSyncError`); only a real failure does. - **Copy and legal:** the privacy policy (Google Sign-In, what sign-out clears, and that shared test results are now always under an account id, since tests need one), the terms, the Account and Your data sections in Settings, and the overview list. Effective date moved to September 19, 2026. - **Nothing is lost on the way out.** Before clearing, the dialog pushes anything still only in this browser to the account. If that fails (offline, or Firebase blocked as in the tests) it says so — "Your account is out of reach… anything it hasn't saved yet would be lost" — and clears only if the person chooses **Sign out anyway**. - **Locked areas are marked** in the menu with a small lock while signed out (`nav-pill.tsx`, `lockedHrefs`), so it's clear before clicking. - **Tests:** unit tests for the rule and for the reset (the database is gone, `solvelab.` keys are gone, the appearance key and other apps' keys stay). For e2e, tests now run as a signed-in person: with Firebase blocked, `tests/e2e/fixtures.ts` seeds the stored session that a real sign-in would leave, so no test hook exists in the app itself. `test.use({ account: "signedOut" })` gets the signed-out site, which `auth.spec.ts` uses to check that the timer works, that all four areas ask for sign-in, and that signing out leaves nothing behind.

125. Owner testing the **live site**: after signing out, their solves were still there, and signing into a **second account** showed the first account's solves and coach. Two separate things. - **The live site is 3.1.** Entries 119–124 (the AI coach, the typed conversation, the account rules and the sign-out clear-out) are committed on `ui-overhaul` and have never been pushed or deployed, so none of that behaviour exists at solvelab.bhargava-gumpula.com yet. On 3.1, signing out only ends the Google session. - **The real hole, which entry 124 did not close:** nothing recorded _whose_ copy the local database was. Signing in merged whatever was in the browser into the account that just signed in — and then uploaded it, so a second account could end up owning the first one's times. Clearing on sign-out narrows the window but doesn't close it: another tab can hold the database open, a tab can crash, or the browser can be closed mid-way. - **The database now knows its owner.** Dexie v7 adds a local-only `meta` table (deliberately not in the sync registry) holding the account id this copy belongs to. On sign-in, `claimAccount` returns **adopted** (no owner: a signed-out person's own times join the account), **same**, or **switched**. On _switched_ the browser is cleared before anything is read or uploaded, and the page starts again, so the new account sees only its own data pulled from the cloud (`lib/storage/account-owner.ts`, `startAccountSession` in `lib/sync/account.ts`, `components/layout/account-sync-provider.tsx`). - **The clear-out is no longer stoppable.** It used to drop the database, which another tab can block. It now empties every table through plain IndexedDB first — which can't be blocked, and which also avoids Dexie's sync hooks, so no deletions are recorded to push to the account — and only then tries to drop the database. - **Tests:** unit tests for adopt/same/switched and that refusing doesn't quietly take the copy over, for the v6→v7 upgrade (version 6 data intact, no owner recorded, so the next sign-in adopts it), and that a clear-out empties the tables even while another connection holds the database open. An end-to-end test signs in, leaves a solve and a conversation, then signs a second account in on the same browser without signing out: no solves, no conversation of the first account's, and the copy now belongs to the second account.

126. Owner: push everything, then start the algorithm bank, with a label on each case for whether you know it, are learning it, or don't know it, and as many working algorithms per case as possible. Pushed `ui-overhaul` (`18ea78c..9f47ddc`); `main` stays at the 3.1 release and nothing was deployed. - **Every algorithm is checked against a cube, not trusted.** `lib/cube/case-check.ts` builds a case by undoing one of its own algorithms on a solved cube, then requires every other algorithm offered for that case to solve it again, allowing a U turn on either side and looking past a whole-cube rotation (published algorithms often end with the cube turned round). A case that disturbs the first two layers is rejected outright, and `orientationSignature` / `caseSignature` catch two entries that are secretly the same case. - **PLL and OLL are in, with 199 algorithms across 78 cases.** PLL: 21 cases, 60 algorithms. OLL: 57 cases, 139 algorithms, and the checker confirms the 57 orientations are all different, which is the whole set. Writing them this way paid for itself immediately: the first pass rejected 14 PLL entries (a wrong Ab, a made-up T variation, and others), which were fixed or dropped rather than shipped. Nothing is published unless the engine solves the case with it. - **The three labels** (`lib/algorithms/labels.ts`): don't know, learning, know it, stored in the synced `algorithmProgress` table through a new repository. The older five-state field is read as one of the three, so nothing is lost. The set page counts them ("1 of 57 known") and can filter by them. - **Your algorithm, not ours.** Each case lists every algorithm that works; "Use this one" remembers your pick and the case card shows it from then on. - **Diagrams come from the data.** `CaseDiagram` draws the last layer from the very cube state the checker produced, so a diagram can't disagree with its algorithm: an orientation case shows which stickers face up, a permutation case shows the side colours you actually recognise it by. - **Tests:** a unit test walks every shipped algorithm through the engine, checks no case is listed twice, that ids never collide and that most cases offer a choice; plus search and the label mapping. An end-to-end test marks a case known, picks the second algorithm, and reloads to find both still there, then filters and searches. - **Still to come in this set:** F2L, 2-look OLL and PLL, COLL and WV. F2L needs its own check (a slot solved with everything else left alone) rather than a last-layer one.

127. Owner: do F2L, then put localhost back up. - **The 41 cases were worked out from the cube, not copied.** An F2L case is any position where the front-right slot is the only thing missing from the first two layers; two positions count as one case when they differ by a turn of the top layer, since that turn is the solver's own. Searching every sequence of R, U and F turns from a solved cube found 36 of them, a handful of classic algorithms revealed 4 more, and walking out from those reached the last two — the ones with both pieces stuck in the slot. Counting the placements confirms the set is complete: 150 ways the pair can sit, 42 once alignment is ignored, minus the one where it is already solved, leaves **41**. - **The algorithms are the shortest that exist** in R, U and F, found by trying every sequence shortest-first: 88 of them, one to four per case, from three moves (`R U R'`) up to nine for the awkward ones. Staying in those three faces keeps the work on the right-hand side and leaves the rest of the first two layers alone, which is the point of an F2L algorithm. - **The checker learned a new shape.** `caseStateOf` and `satisfies` take the kind of case: for a pair case, everything except the front-right slot must be there to begin with, and afterwards the first two layers must be finished, with the last layer left however it lies (`otherSlotsSolved`, `lib/cube/case-check.ts`). - **The diagrams grey out the noise.** In a pair case the last layer is meaningless, so only the pair's own stickers are coloured, and the front-right slot is drawn underneath as two pairs of stickers seen edge on. Each case also says in words where the pair is ("Corner at the back left, its bottom colour pointing up. Edge on the right, its front colour pointing up."), worked out from the position rather than written by hand. - **Totals:** the bank is now 119 cases and 311 algorithms across PLL, OLL and F2L, every one checked against the cube engine by `tests/unit/algorithms.test.ts`.

128. Owner: fill in the sets that are left — the two-look ones, then COLL and Winter Variation — and leave the other two. - **2-look OLL and 2-look PLL are made of cases that already exist.** A case can now point at another set's case (`sameAs`), so it shares that case's algorithms and its label: marking Sune known in 2-look OLL marks it known in Full OLL, because knowing Sune is knowing Sune. 2-look OLL is three edge-orientation cases of its own (dot, line, L) plus the seven corner cases from OLL 21–27; 2-look PLL is the three corner-permutation cases (Aa, Ab, E) and the four edge ones (Ua, Ub, Z, H), all borrowed from Full PLL. Cases can also carry their own `kind`, because the first step of 2-look OLL finishes only the edges — a new check (`eoll`). Two entries I wrote from memory were wrong (an L-shape algorithm filed under the line, and an A perm filed as a corner swap); the checker caught both. - **COLL: 40 cases, 156 algorithms.** The community's algorithms, collected by SpeedCubeDB, run through the engine: each has to solve the case it is listed under, with corners home and oriented and the edges left oriented but free. All 156 passed, none dropped, and a corner-only signature confirmed the 40 cases are all different. - **Winter Variation: 27 cases.** Same source, same treatment, with a new check (`wv`): the case starts a pair short like an F2L case, and afterwards the whole last layer must face up. All 27 passed. The set's premise — that the last layer's edges are already up — was checked too, once the pair's own edge was excluded from the count. - **Diagrams** show the empty slot for Winter Variation as well as F2L, but don't grey anything there, since the corners it is about to bring up are the point. - `npm run ml:coll` and `npm run ml:wv` rebuild those two sets from their lists. - **Left alone, as asked:** Fundamentals and ZBLL. - **The bank now:** 233 cases and 494 algorithms across eight sets, every one checked against the cube.

129. Owner: an algorithm shouldn't start by turning the cube round. A leading `y` only says "hold it the other way", which the case picture already shows, so OLL 1's second algorithm reading `y R U B' R B …` was wrong to print. An `x` or a `z` is different: it tips the cube onto another layer, so it is part of the algorithm and stays. - **125 algorithms started with a y** (84 in COLL, 31 in OLL, 10 in PLL), because that is how published lists say "held this way round". Each was fixed in whichever way still solves the pictured case, checked against the engine before being written down: **94 of them simply lost the turn**, which was redundant — turning the whole cube and turning only the top layer come to the same thing for a last-layer case, and lining the case up is the solver's own move either way. The other **31 turned out to be an algorithm the case already listed** once the turn was gone, so they went. None needed rewriting onto different faces, and none had to be kept as they were. - The bank is now 233 cases and **463 algorithms**, none of which begins with a cube rotation; rotations in the middle are untouched, since they are part of the fingers' work. A unit test keeps it that way, and also checks rotations still appear elsewhere, so the rule can't quietly grow teeth. - `ml/fix-rotations.ts` did the work and stays in the repo.

130. Owner feedback, four things. - **Old solves followed them into an account.** Signing in used to adopt whatever was in the browser, which is right for someone who timed a few solves before signing up but wrong when the data was an earlier account's. Now an unclaimed copy is only adopted when the account itself is empty; an account that already has times of its own starts the browser clean and takes what the cloud holds (`startAccountSession`, `hasOwnData`). A default session and settings don't count as data, so a fresh copy still counts as empty. - **The chat coach is gone.** The owner's verdict: "the coach is now completely useless… I would prefer the previous version if you don't connect your own AI." `components/coach/coach-dashboard.tsx` is back from before phase 3 — goal, the next test and why, the solve profile, the daily check, and what to work on first — now carrying the richer per-part tips written in phase 3 (why, three tips, a drill, sources) instead of the old stage tips. The page ends by saying that asking questions in your own words needs an AI of your own, linking to Settings. The conversation, engine and model stay in the repo unused: they are what a connected AI will drive. "Retest my weak spots", which the owner couldn't make sense of, went with the chat. - **Algorithms, three changes.** The filter is now any mix of the three labels rather than one at a time; clicking a case moves it round don't know → learning → know it, and the whole card takes that colour; a small, faint pencil in the corner opens the case to pick an algorithm. Button rather than right-click on purpose: a context menu is invisible until you try it, has no touch equivalent, and fights the browser's own menu. - **Diagrams show the colours a case actually fixes.** They were white-on-grey, which looks nothing like the cube in your hands, because the engine calls the top face U and U is white. Pictures now follow the hands: the last layer is yellow. Beyond that a sticker is only coloured when the case pins it down — an orientation case says nothing about where pieces go, so its sides stay grey; a permutation case is read from exactly those colours; COLL fixes the corners but lets the edges sit anywhere; a pair case fixes the pair alone. Grey means "any colour", which is the honest thing for a diagram to say.
